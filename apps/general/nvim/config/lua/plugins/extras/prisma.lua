-- Fully covered already: prismals in lsp.lua (formats .prisma via LSP
-- fallback in conform), prisma treesitter parser in coding.lua, and
-- dadbod in tools.lua for querying the underlying database.
return {}
